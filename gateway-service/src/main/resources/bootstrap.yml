spring:
  application:
    name: gateway-service
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      fetchRegistry: false