server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gvardianez/market-configs.git
  application:
    name: config-server
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      fetchRegistry: false

